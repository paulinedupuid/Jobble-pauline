<nav class="tabbar">

  <%= link_to dashboard_path(@user), class: "tabbar-icons" do %>
  <%# ❗️ ajout de ligne pour editer la home à remplacer par la nouvelle %>
  <%= render '/shared/svg/dashboard.svg' %>
  <p>Mon Profil</p>
  <% end %>

  <%= link_to offers_path, class: "tabbar-icons" do%>
  <%= render '/shared/svg/matchs.svg' %>
  <p>Swipez!</p>
  <% end %>
  <% message_count = 0 %>
  <% chatrooms = Chatroom.all %>
  <% chatrooms.each do |chatroom| %>
  <%# aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa %>
      <% if chatroom.messages %>
        <%  chatroom.messages.each do |message| %>

              <%# dans boucle message %>
              <% unless message.user_id == current_user.id %>
                  <% if message.status == false %>
                  <% message_count += 1 %>
                  <% end %>
              <% end %>

        <% end %> <%# end du each message%>
      <% end %> <%# end de la condi  %>
  <% end %> <%# end du each chatroom %>

  <%# aqaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa %>
  <%# a finir le insert du data set %>
<div id='message-notif' data-message= "<%= message_count %>">

<div class='test'>

</div>
</div>
</div>
  <a href="/message_index" class="tabbar-icons">
  <%= render '/shared/svg/messages.svg' %>
    <p>Messages</p>
  </a>
</nav>
<%# <%= raise %>
